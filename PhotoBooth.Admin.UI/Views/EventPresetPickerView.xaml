<UserControl x:Class="PhotoBooth.Admin.UI.Views.EventPresetPickerView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Background="{StaticResource BackgroundBrush}">
 <Grid Margin="28,24,28,28">
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <Grid Margin="0,0,0,16"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Chọn preset cho event" FontSize="28" FontWeight="Bold"/><TextBlock Text="Chọn các hiệu ứng màu sẽ được sử dụng trong event." Foreground="#A9AAB1" Margin="0,5,0,0"/></StackPanel><Border Grid.Column="1" Background="#263D64" CornerRadius="14" Padding="11,5" VerticalAlignment="Center"><TextBlock Text="{Binding SelectionSummary}" Foreground="#BDD4FF"/></Border></Grid>
  <TextBox Grid.Row="1" Text="{Binding Search,UpdateSourceTrigger=PropertyChanged}" ToolTip="Tìm preset theo tên hiển thị" Margin="0,0,0,12" Padding="12,9"/>
  <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="10"><ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><ItemsControl ItemsSource="{Binding VisibleItems}"><ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel><ItemsControl.ItemTemplate><DataTemplate><Button Command="{Binding DataContext.ToggleCommand,RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Width="220" Height="250" Padding="0" Margin="7" Background="Transparent"><Button.Template><ControlTemplate TargetType="Button"><Border x:Name="Card" Background="#18181B" BorderBrush="#44444B" BorderThickness="2" CornerRadius="10" ClipToBounds="True"><Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><Image Source="{Binding PreviewBytes,Converter={StaticResource ByteImage}}" Stretch="Uniform" Margin="8"/><Border Grid.Row="1" Background="#242428" Padding="10,9"><TextBlock Text="{Binding Name}" TextAlignment="Center" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/></Border><Border x:Name="SelectedBadge" Visibility="Collapsed" Background="#E64F8CFF" CornerRadius="13" Width="28" Height="28" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8"><TextBlock Text="✓" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/></Border></Grid></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Card" Property="BorderBrush" Value="#759FFF"/></Trigger><DataTrigger Binding="{Binding IsSelected}" Value="True"><Setter TargetName="Card" Property="BorderBrush" Value="#4F8CFF"/><Setter TargetName="Card" Property="Background" Value="#243452"/><Setter TargetName="SelectedBadge" Property="Visibility" Value="Visible"/></DataTrigger></ControlTemplate.Triggers></ControlTemplate></Button.Template></Button></DataTemplate></ItemsControl.ItemTemplate></ItemsControl></ScrollViewer></Border>
  <Grid Grid.Row="3" Margin="0,14,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><Border Background="#3A321F" BorderBrush="#6A582B" BorderThickness="1" CornerRadius="7" Padding="11,9" Visibility="{Binding HasMessage,Converter={StaticResource BoolVisibility}}"><TextBlock Text="{Binding Message}" Foreground="#FFD98A" TextWrapping="Wrap"/></Border><WrapPanel Grid.Column="1"><Button Content="Hủy" Command="{Binding CancelCommand}" Background="#45454B" MinWidth="100"/><Button Content="Dùng các preset đã chọn" Command="{Binding ApplyCommand}" Background="#267454" MinWidth="200"/></WrapPanel></Grid>
 </Grid>
</UserControl>
