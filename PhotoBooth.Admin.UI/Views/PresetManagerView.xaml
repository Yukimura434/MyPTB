<UserControl x:Class="PhotoBooth.Admin.UI.Views.PresetManagerView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"><Grid Margin="20"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><TextBlock Text="Preset Manager" Style="{StaticResource PageTitle}"/><Grid Grid.Row="1"><Grid.ColumnDefinitions><ColumnDefinition Width="3*"/><ColumnDefinition Width="2*"/></Grid.ColumnDefinitions><Border Style="{StaticResource Card}"><ListBox ItemsSource="{Binding Presets}" SelectedItem="{Binding SelectedPreset}"><ListBox.ItemTemplate><DataTemplate><Border Background="#252526" CornerRadius="8" Padding="14" Margin="3"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"/><TextBlock Foreground="#999"><Run Text="Created "/><Run Text="{Binding CreatedAtUtc}"/><Run Text="  ·  Modified "/><Run Text="{Binding ModifiedAtUtc}"/></TextBlock></StackPanel><Border Grid.Column="1" Background="#4CAF50" CornerRadius="10" Padding="10,3" Visibility="{Binding IsDefault,Converter={StaticResource BoolVisibility}}"><TextBlock Text="DEFAULT" FontSize="10"/></Border></Grid></Border></DataTemplate></ListBox.ItemTemplate></ListBox></Border><Border Grid.Column="1" Style="{StaticResource Card}"><StackPanel><TextBlock Text="Preset Details" Style="{StaticResource SectionTitle}"/><TextBlock Text="Name"/><TextBox Text="{Binding NewName,UpdateSourceTrigger=PropertyChanged}"/><WrapPanel><Button Content="Create" Command="{Binding CreateCommand}"/><Button Content="Rename" Command="{Binding RenameCommand}"/><Button Content="Duplicate" Command="{Binding DuplicateCommand}"/><Button Content="Set Default" Command="{Binding DefaultCommand}" Background="#4CAF50"/><Button Content="Delete" Command="{Binding DeleteCommand}" Background="#F44336"/></WrapPanel><TextBlock Text="{Binding Message}" Foreground="#FFB74D" Margin="4,14"/><TextBlock Text="Image processing is intentionally not configured in this release." Foreground="#999" TextWrapping="Wrap"/></StackPanel></Border></Grid></Grid></UserControl>
